An implementation of the Money Transfer DCI example in ruby.

This shows an implementation without injection, where role methods
are public class methods.

See also the injection branch, where objects are extended with role modules.

See in app/contexts, app/models and lib/ 
