An implementation of the Money Transfer DCI example in ruby.
This branch injects role functionality into objects.
The master branch does it without injection (where role methods are public class methods).

See app/contexts, app/models and lib/ 
